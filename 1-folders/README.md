# 1-folders

Creates the organization hierarchy after the remote state backend is available.
Use parent_id only when creating a folder beneath another folder in the same
apply; otherwise the folder is created directly beneath the organization.
