# 3-resources

Deploys shared resources after their owning projects exist. The initial
composition creates hardened GCS buckets; add further resource modules here
instead of coupling workload resources to organization bootstrap.
